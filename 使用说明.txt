芒柠影业 · 进一步压缩版

保留原来选定的 13 张横屏照片和顺序。
原始 JPG 照片未修改或删除。

压缩参数：WebP 格式，桌面及放大图最长边 1280 像素、质量 64；手机图最长边 640 像素、质量 60；缩略图最长边 160 像素、质量 55。
压缩时使用原始照片重新生成，避免对已经压缩的图片再次转码。
这是有损压缩，放大查看的细节会少一些。打印或后期编辑请继续使用原始照片。

全部网页图片（含三个尺寸）由 2.98 MB 降至 1.47 MB，减少 50.6%。
13 张原始照片合计 39.85 MB；13 张桌面压缩图合计 1.14 MB。
MB 按 1024 × 1024 字节计算。

解压整个网页包后打开 index.html。发布时上传 HTML、CSS、JS、Logo、favicon 以及 assets 文件夹。
图片引用的尺寸和缓存版本已同步更新，需一起替换 HTML 和图片。
保留图片在页面上的显示大小、每 5 秒轮播、放大查看、MNfilm 英文名、单个 Logo 和关于我们的光影背景。
